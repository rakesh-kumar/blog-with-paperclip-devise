<h1>Listing homes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>attachment</th>
    <th>picture</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @homes.each do |home| %>
  <tr>
    <td><%= home.name %></td>
    <td><%= home.title %></td>
     <td><%= home.attachment %></td>
      <td><%= home.picture %></td>
    <td><%= link_to 'Show', home %></td>
    <td><%= link_to 'Edit', edit_home_path(home) %></td>
    <td><%= link_to 'Destroy', home, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Home', new_home_path %>
